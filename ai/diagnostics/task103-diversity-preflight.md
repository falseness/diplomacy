TASK-103 finite no-fit preflight

Hypothesis: numeric seed separation concealed repeated physical startup inputs;
the inherited checkpoint and prior development corpus must be included before
any further outcome fit can support generalization. This is a data prerequisite,
not a change to the policy or evidence of strength.

Run `node ai/diagnostics/task103-diversity-preflight.cjs FRESH_OUTPUT_DIRECTORY`.
The command never loads a checkpoint, starts a game, collects labels, or fits.
It inventories saved JSON map objects under artifacts/TASK-103, recording file
hashes, JSON locations, duplicate groups and incomplete records. Large vector
arrays are not map objects. Symlinks and source/dependency trees are excluded
and reported. Compressed traces and external storage are explicitly outside this
JSON inventory: absence there cannot establish complete prior exposure.

Recover the economy-development checkpoint's ten training/validation startup
maps from its frozen generator, and regenerate archived stage-2 layouts only
when the generator matches the recorded source hash. Serialize constructor names
explicitly. Do not infer that omitted types in arbitrary saved maps were Noob:
GameMap defaults missing types to Noob, but JSON also erases other constructors.
Hash canonical startup fields with entity array order preserved. Also compare
geometry with sudden-death omitted to prevent budget differences masquerading
as independent maps. Never change the actual budgets to eliminate collisions.

Predeclared richer construction, not authorized training: twelve native 9x7
maps based on stage-2 seed 10311000, with one hill at each successive coordinate
in [(0,0)..(8,0), (0,6)..(2,6)]. Keep every entity, its order, gold110 per side,
classes AIPlayerWithEconomy/SimpleAiPlayerWithEconomy and action/command budgets
30/60 fixed; round limit1200 and sudden death500. Hills change legitimate terrain
at otherwise vacant boundary cells. Indices0..7 train,8..9 validation,10..11
development, both sides; minimum physical split sizes8/2/2. No replacement
coordinates or seeds after outcomes or a collision. Acceptance remains separate.

Decision: capacity8 rejects the old twelve-layout premise. Stop with exit2 if
any prior exposure or startup semantics remain unknown, any split collides, or
runtime round-trip equality is unproved. The current corpus has no complete
exposure ledger; report that limit explicitly rather than declaring it unseen.
Only after a complete corpus and startup validation may this plan be frozen as
eligible for a separately declared matched initial/outcome/zero experiment.
This command deliberately has no training continuation. A failed preflight
leaves TASK-103 pending and does not permit unchanged acceptance reruns.
