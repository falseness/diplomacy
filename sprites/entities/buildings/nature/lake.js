class Lake extends StaticNature {
    constructor(x, y) {
        const name = 'lake'
        super(x, y, name)
    }
}
class Sea extends StaticNatures {
    constructor(x, y) {
        const name = 'sea'
        super(x, y, name)
    }
}