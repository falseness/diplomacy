class Lake extends Nature {
    constructor(x, y) {
        const name = 'lake'
        super(x, y, name)
    }
}
class Sea extends Nature {
    constructor(x, y) {
        const name = 'sea'
        super(x, y, name)
    }
}