class Normchel extends Unit {
    constructor(x, y) {
        const hp = 6
        const healSpeed = 1
        const dmg = 2
        const speed = 2
        const salary = 4
        super(x, y, 'normchel', hp, healSpeed, dmg, speed, salary)
    }
}