class Normchel extends Unit {
    constructor(x, y) {
        const hp = 4
        const healSpeed = 1
        const dmg = 2
        const speed = 2
        const salary = 6
        super(x, y, 'normchel', hp, healSpeed, dmg, speed, salary)
    }
}