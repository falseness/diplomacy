# TASK-225 three-match supplement

The TASK-225 supplemental producer keeps two H2 co-op matches and a competitive
control alive for two interleaved rounds, using four browser contexts and two
additional protocol identities. It delegates to the real MongoDB driver and AI,
records await entry/exit, full inbound boards and intermediate documents, and
uses four individually enclosed imps on each declared seed-1 tiny co-op board.
Run into a fresh directory:

```sh
python3 ops/run_three_match_review.py artifacts/TASK-225/review-<fresh>
THREE_MATCH_ARCHIVE=/absolute/completed/supplement python3 -m unittest discover -s ops -p test_review_three_match.py -v
```

The supervisor bounds the entire producer with an inherited cumulative deadline
and identity-checked process cleanup. The runner freezes exact source hashes,
rejects TASK-209's sequential historical trace, checks independent literal board
oracles, and rejects deleted/swapped proof copies. This is a prerequisite
experiment, not the full evidence-audit entry point. Its `supplement-budget.json`
and `independent-review.json` do not satisfy the full-invocation artifact names.
The runner's first run, review-32, observed genuine AI calls of 8–106 ms and real
cross-game database interleaving. It does not establish a long busy demon phase;
G09 and the whole-criterion consumer comparison remain unresolved. Preserve the
existing 556-check historical oracle and all eight TASK-209 review obligations.
Do not refresh providers or consume a whole G09 review on the strength of this
partial observation. Archive controls check board semantics independently after
changing both received/projection fields, not just saved expected/observed pairs.
